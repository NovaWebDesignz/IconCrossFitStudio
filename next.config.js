/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'source.unsplash.com', 'images.unsplash.com'], // Allow Unsplash & Pexels
  },
};

module.exports = nextConfig;
